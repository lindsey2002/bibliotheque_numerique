name: bibliotheque_numerique_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  serverpod: 3.4.11
  serverpod_auth_idp_server: 3.4.11

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.11
  test: ^1.25.5
  serverpod_swagger: ^0.3.0

serverpod:
  scripts:
    # Starts the server and applies migrations
    start: dart bin/main.dart --apply-migrations

    # Build the Flutter web app and copy it to the server's web directory.
    # Windows uses xcopy since flutter's --output flag doesn't work there.
    # See: https://github.com/flutter/flutter/issues/157886
    flutter_build:
      windows: >-
        (if exist web\app rmdir /S /Q web\app)
        & cd /D ..\bibliotheque_numerique_flutter
        && flutter build web --base-href /app/ --wasm
        && xcopy /E /I /Y build\web ..\bibliotheque_numerique_server\web\app
      posix: |
        cd ../bibliotheque_numerique_flutter
        flutter build web --base-href /app/ --wasm --output ../bibliotheque_numerique_server/web/app
